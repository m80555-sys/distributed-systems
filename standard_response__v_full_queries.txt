standard_response__v full queries

1. Record Count Verification

Source:

SELECT
    id
FROM standard_response__v;

Target:

SELECT
    COUNT(*) AS record_count
FROM raw_vmis.standard_response__v
WHERE hard_deleted_flag = 'N';


2. Uniqueness Verification

SELECT
    id,
    COUNT(*) AS duplicate_count
FROM raw_vmis.standard_response__v
WHERE hard_deleted_flag = 'N'
GROUP BY
    id
HAVING COUNT(*) > 1;


3. Not Null Verification

SELECT *
FROM raw_vmis.standard_response__v
WHERE id IS NULL
   OR insrt_ts IS NULL
   OR updt_ts IS NULL
   OR hard_deleted_flag IS NULL;


4. Data Verification

Source: Simple Select Query

SELECT
    global_id__sys,
    link__sys,
    name__v,
    guidelines_for_use__v,
    standard_response_notes__v,
    standard_response_number__c,
    created__c,
    created_by__v,
    modified_by__v,
    state_stage_id__sys,
    stage__sys,
    email_template__v,
    language__v,
    primary_standard_response__v,
    primary_affiliate__c,
    primary_country__c,
    old_primary_document__c,
    primary_product__c,
    primary_document__c,
    created_date__v,
    modified_date__v,
    id,
    status__v,
    category__c,
    requestor_class__c,
    topic__c,
    response_content__v,
    lifecycle__v,
    state__v,
    primary_document_unbound__c
FROM standard_response__v;

Target: Query fetched from TS03 PDF

SELECT
    global_id__sys,
    link__sys,
    name__v,
    guidelines_for_use__v,
    standard_response_notes__v,
    standard_response_number__c,
    created__c,
    created_by__v,
    modified_by__v,
    state_stage_id__sys,
    stage__sys,
    email_template__v,
    language__v,
    primary_standard_response__v,
    primary_affiliate__c,
    primary_country__c,
    old_primary_document__c,
    primary_product__c,
    primary_document__c,
    SUBSTRING(DATE_FORMAT(created_date__v, 'yyyy-MM-dd HH:mm:ss.SSS'), 1, 10) || 'T' || SUBSTRING(DATE_FORMAT(created_date__v, 'yyyy-MM-dd HH:mm:ss.SSS'), 12, 8) || 'Z' AS created_date__v,
    SUBSTRING(DATE_FORMAT(modified_date__v, 'yyyy-MM-dd HH:mm:ss.SSS'), 1, 10) || 'T' || SUBSTRING(DATE_FORMAT(modified_date__v, 'yyyy-MM-dd HH:mm:ss.SSS'), 12, 8) || 'Z' AS modified_date__v,
    id,
    status__v,
    category__c,
    requestor_class__c,
    topic__c,
    response_content__v,
    lifecycle__v,
    state__v,
    primary_document_unbound__c
FROM raw_vmis.standard_response__v
WHERE hard_deleted_flag = 'N';
