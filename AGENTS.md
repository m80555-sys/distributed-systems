# AGENTS.md

## Cursor Cloud specific instructions

This repository (`distributed-systems`) is currently an empty/newly initialized project with only a `README.md`. There is no application code, no package manager, no build system, no tests, and no services defined yet.

When application code is added, update this section with:
- How to install dependencies
- How to run, build, lint, and test the application
- Any non-obvious development environment caveats
