survey_response__c full queries

1. Record Count Verification

Source:

SELECT
    id
FROM survey_response__c;

Target:

SELECT
    COUNT(*) AS record_count
FROM raw_vmis.survey_response__c
WHERE hard_deleted_flag = 'N';


2. Uniqueness Verification

SELECT
    id,
    COUNT(*) AS duplicate_count
FROM raw_vmis.survey_response__c
WHERE hard_deleted_flag = 'N'
GROUP BY
    id
HAVING COUNT(*) > 1;


3. Not Null Verification

SELECT *
FROM raw_vmis.survey_response__c
WHERE id IS NULL
   OR insrt_ts IS NULL
   OR updt_ts IS NULL
   OR hard_deleted_flag IS NULL;


4. Data Verification

Source: Simple Select Query

SELECT
    are_you_healthcare_provider__c,
    case__c,
    case_owner__c,
    created_by__v,
    created_date__v,
    easy_to_contact_abbvie__c,
    expected_timeframe__c,
    global_id__sys,
    id,
    lifecycle__v,
    link__sys,
    medinfo_helped_informed_decision__c,
    modified_by__v,
    modified_date__v,
    name__v,
    quality_of_info_rating__c,
    response_within_expected_timeframe__c,
    stage__sys,
    state__v,
    state_stage_id__sys,
    status__v,
    was_medical_information_provided_useful__c,
    website_awareness__c,
    comment_for_improvement__c,
    how_to_improve_scientific_info_provided__c,
    why_was_it_difficult_to_contact_abbvie__c,
    why_was_the_info_provided_not_useful__c
FROM survey_response__c;

Target: Query fetched from TS03 PDF

SELECT
    are_you_healthcare_provider__c,
    case__c,
    case_owner__c,
    created_by__v,
    SUBSTRING(date_format(created_date__v, 'YYYY-MM-dd HH:mm:ss.SSS'),1,10)||'T'||SUBSTRING(date_format(created_date__v, 'YYYY-MM-dd HH:mm:ss.SSS'),12,8)||'Z' as created_date__v,
    easy_to_contact_abbvie__c,
    expected_timeframe__c,
    global_id__sys,
    id,
    lifecycle__v,
    link__sys,
    medinfo_helped_informed_decision__c,
    modified_by__v,
    SUBSTRING(date_format(modified_date__v, 'YYYY-MM-dd HH:mm:ss.SSS'),1,10)||'T'||SUBSTRING(date_format(modified_date__v, 'YYYY-MM-dd HH:mm:ss.SSS'),12,8)||'Z' as modified_date__v,
    name__v,
    quality_of_info_rating__c,
    response_within_expected_timeframe__c,
    stage__sys,
    state__v,
    state_stage_id__sys,
    status__v,
    was_medical_information_provided_useful__c,
    website_awareness__c,
    CAST(aes_decrypt(unbase64(comment_for_improvement__c), "SaGRHRWdRV05qWlhOeklpQTZJSHNnSW0") AS STRING) AS comment_for_improvement__c,
    CAST(aes_decrypt(unbase64(how_to_improve_scientific_info_provided__c), "SaGRHRWdRV05qWlhOeklpQTZJSHNnSW0") AS STRING) AS how_to_improve_scientific_info_provided__c,
    CAST(aes_decrypt(unbase64(why_was_it_difficult_to_contact_abbvie__c), "SaGRHRWdRV05qWlhOeklpQTZJSHNnSW0") AS STRING) AS why_was_it_difficult_to_contact_abbvie__c,
    CAST(aes_decrypt(unbase64(why_was_the_info_provided_not_useful__c), "SaGRHRWdRV05qWlhOeklpQTZJSHNnSW0") AS STRING) AS why_was_the_info_provided_not_useful__c
FROM raw_vmis.survey_response__c
WHERE hard_deleted_flag = 'N';
