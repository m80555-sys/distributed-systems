fulfillment_document__v full queries

1. Record Count Verification

Source:

SELECT
    id
FROM fulfillment_document__v;

Target:

SELECT
    COUNT(*) AS record_count
FROM raw_vmis.fulfillment_document__v
WHERE hard_deleted_flag = 'N';


2. Uniqueness Verification

SELECT
    id,
    COUNT(*) AS duplicate_count
FROM raw_vmis.fulfillment_document__v
WHERE hard_deleted_flag = 'N'
GROUP BY
    id
HAVING COUNT(*) > 1;


3. Not Null Verification

SELECT *
FROM raw_vmis.fulfillment_document__v
WHERE id IS NULL
   OR insrt_ts IS NULL
   OR updt_ts IS NULL
   OR hard_deleted_flag IS NULL;


4. Data Verification

Source: Simple Select Query

SELECT
    attachment__v,
    case_response__v,
    created_by__v,
    created_date__v,
    document__v,
    document_order__v,
    document_type_detail__c,
    document_unbound__v,
    fulfillment_document_type__c,
    global_id__sys,
    id,
    lifecycle__v,
    link__sys,
    modified_by__v,
    modified_by__v,
    modified_date__v,
    name__v,
    object_type__v,
    source_attachment_id__v,
    stage__sys,
    standard_response_document__v,
    state__v,
    state_stage_id__sys,
    status__v,
    val_pd__c
FROM fulfillment_document__v;

Target: Query fetched from TS03 PDF

SELECT
    attachment__v,
    case_response__v,
    created_by__v,
    SUBSTRING(date_format(created_date__v, 'yyyy-MM-dd HH:mm:ss.SSS'),1,10)||'T'||SUBSTRING(date_format(created_date__v, 'YYYY-MM-dd HH:mm:ss.SSS'),12,8)||'Z' as created_date__v,
    document__v,
    document_order__v,
    document_type_detail__c,
    document_unbound__v,
    fulfillment_document_type__c,
    global_id__sys,
    id,
    lifecycle__v,
    link__sys,
    modified_by__v,
    modified_by__v,
    SUBSTRING(date_format(modified_date__v, 'yyyy-MM-dd HH:mm:ss.SSS'),1,10)||'T'||SUBSTRING(date_format(modified_date__v, 'YYYY-MM-dd HH:mm:ss.SSS'),12,8)||'Z' as modified_date__v,
    name__v,
    object_type__v,
    source_attachment_id__v,
    stage__sys,
    standard_response_document__v,
    state__v,
    state_stage_id__sys,
    status__v,
    val_pd__c
FROM raw_vmis.fulfillment_document__v
WHERE hard_deleted_flag = 'N';
