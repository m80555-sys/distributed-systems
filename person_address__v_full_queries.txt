person_address__v full queries

1. Record Count Verification

Source:

SELECT
    id
FROM person_address__v;

Target:

SELECT
    COUNT(*) AS record_count
FROM raw_vmis.person_address__v
WHERE hard_deleted_flag = 'N';


2. Uniqueness Verification

SELECT
    id,
    COUNT(*) AS duplicate_count
FROM raw_vmis.person_address__v
WHERE hard_deleted_flag = 'N'
GROUP BY
    id
HAVING COUNT(*) > 1;


3. Not Null Verification

SELECT *
FROM raw_vmis.person_address__v
WHERE id IS NULL
   OR insrt_ts IS NULL
   OR updt_ts IS NULL
   OR hard_deleted_flag IS NULL;


4. Data Verification

Source: Simple Select Query

SELECT
    link__sys,
    email__v,
    extension__v,
    external_id__v,
    fax__v,
    latitude_cda__v,
    longitude_cda__v,
    phone__v,
    profile__v,
    veeva_id__v,
    primary__v,
    object_type__v,
    created_by__v,
    modified_by__v,
    country__v,
    state_stage_id__sys,
    stage__sys,
    country__c,
    created_date__v,
    modified_date__v,
    last_crm_data_share_date__v,
    id,
    status__v,
    address_type__v,
    source__v,
    lifecycle__v,
    state__v
FROM person_address__v;

Target: Query fetched from TS03 PDF

SELECT
    link__sys,
    email__v,
    extension__v,
    external_id__v,
    fax__v,
    latitude_cda__v,
    longitude_cda__v,
    phone__v,
    profile__v,
    veeva_id__v,
    primary__v,
    object_type__v,
    created_by__v,
    modified_by__v,
    country__v,
    state_stage_id__sys,
    stage__sys,
    country__c,
    SUBSTRING(DATE_FORMAT(created_date__v, 'yyyy-MM-dd HH:mm:ss.SSS'), 1, 10) || 'T' || SUBSTRING(DATE_FORMAT(created_date__v, 'yyyy-MM-dd HH:mm:ss.SSS'), 12, 8) || 'Z' AS created_date__v,
    SUBSTRING(DATE_FORMAT(modified_date__v, 'yyyy-MM-dd HH:mm:ss.SSS'), 1, 10) || 'T' || SUBSTRING(DATE_FORMAT(modified_date__v, 'yyyy-MM-dd HH:mm:ss.SSS'), 12, 8) || 'Z' AS modified_date__v,
    SUBSTRING(DATE_FORMAT(last_crm_data_share_date__v, 'yyyy-MM-dd HH:mm:ss.SSS'), 1, 10) || 'T' || SUBSTRING(DATE_FORMAT(last_crm_data_share_date__v, 'yyyy-MM-dd HH:mm:ss.SSS'), 12, 8) || 'Z' AS last_crm_data_share_date__v,
    id,
    status__v,
    address_type__v,
    source__v,
    lifecycle__v,
    state__v
FROM raw_vmis.person_address__v
WHERE hard_deleted_flag = 'N';
